<%= form_for (@item) do |f| %>
  <div class="form-group">
    <%= f.label :task %>
    <%= f.text_field :task, class: 'form-control', placeholder: "Enter the task" %>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
