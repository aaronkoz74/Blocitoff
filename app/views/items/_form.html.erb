<%= form_for [user, item] do |f| %>
  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: 'form-control', placeholder: "Enter the task" %>
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
